{
  "name": "qa-bdd-cypress-saucedemo",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress + Cucumber (BDD) starter with POM for SauceDemo v1",
  "license": "MIT",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "format": "prettier --write .",
    "lint": "eslint . --ext .ts,.js"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.1.2",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.6",
    "@types/node": "^20.19.23",
    "cypress": "^13.17.0",
    "esbuild": "^0.24.2",
    "eslint": "^9.38.0",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
